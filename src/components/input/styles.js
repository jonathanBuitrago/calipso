import { StyleSheet } from "react-native";

export default StyleSheet.create({
  input: {
    alignSelf: "stretch",
    borderWidth: 1,
    borderColor: "#acacac",
    marginTop: 10,
    padding: 10
  }
});
